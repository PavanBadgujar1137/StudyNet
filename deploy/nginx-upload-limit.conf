# Included in http {} on Amazon Linux (conf.d).
# 10-hour 1080p lecture can be ~15–40GB; 50G covers that.
# Timeouts allow slow client uploads (hours), not just file size.
client_max_body_size 50G;
client_body_timeout 14400s;
client_header_timeout 300s;
send_timeout 14400s;
